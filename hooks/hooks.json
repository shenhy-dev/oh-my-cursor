{
  "version": 1,
  "hooks": {
    "beforeShellExecution": [
      { "command": "bash \"${CURSOR_PLUGIN_ROOT}/hooks/guard-shell.sh\"", "failClosed": true }
    ],
    "afterFileEdit": [
      { "command": "bash \"${CURSOR_PLUGIN_ROOT}/hooks/post-edit-lint.sh\"" }
    ]
  }
}
